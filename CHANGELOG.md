# Изменения

## CV-VI-ver.3.3-fixes (от CV-VI-ver.3.2-autoThinking)

Исправления и доработки по результатам работы с 3.2. В `main` не влита.

### 3.3.1 — файлы данных: CSV, TXT, Excel и проверка

- Загрузка ВАХ и ВФХ из `.csv`, `.txt`, `.dat`, `.tsv`, `.xlsx`, `.xlsm`
  (`.xls` — если установлен `xlrd`, иначе подсказка сохранить как `.xlsx`).
  Разделитель столбцов и десятичная запятая определяются сами; кодировки
  UTF-8 и Windows-1251; в Excel берётся лист с данными.
- Заголовок столбцов распознаётся: столбцы V/I/C выбираются по названию
  (можно больше двух столбцов), единицы (мВ, мА, мкА, нА, пФ, нФ …)
  переводятся в СИ.
- Модуль `datafile.py` проверяет данные и объясняет замечания: что не так,
  что сделать, пункт методички (п. 1А.17). Ошибки (файл не загружен):
  формат, нет чисел, неоднозначная запятая, перепутаны столбцы, плотность
  тока вместо тока, постоянный столбец. Предупреждения (данные загружены):
  мало точек, повторы и гистерезис, нечисловые строки, нет прямой или
  обратной ветви, обратный ток по модулю (знак ставится), обратная
  полярность (ток ×(−1)), нет выпрямления, подозрительные величины
  (вероятно, не те единицы), ёмкость ≤ 0. Предупреждения по данным видны во
  вкладке «Предупреждения».
- «Данные → Формат файлов данных и примеры» (и «Справка → Формат файлов
  данных»): описание формата, каталог примеров, «Проверить файл...» — разбор
  без загрузки.
- Примеры (синтетика): `mesa_diode/simulator/examples/` — ВАХ в CSV, TXT с
  десятичной запятой и мкА, Excel с тремя столбцами и мА; ВФХ в пФ; описание
  формата — `examples/README.md`. Примеры входят в сборку `.exe`.
- Зависимость `openpyxl`.

### 3.3.2 — ρ подложки в базовой модели и фиксация параметров

- Поле ρ_sub открыто в базовом режиме: в сценарии B (по умолчанию) подложка —
  p-сторона перехода, и по ρ считаются V_bi, ширина ОПЗ и ВФХ. На ВАХ
  базовой модели (формула (6.3)) ρ не влияет.
- Галочка справа от каждого поля — «зафиксировать при подгонке»: подгонка
  оставляет введённое значение и подбирает остальные (n, J₀, τ₀^bg,
  τ^bg, R_s, I_mod, R_sh, I_L, m). Серые галочки — параметры, которые
  подгонка не меняет никогда. I_L = 0 или I_mod = inf с галочкой выключают
  механизм, ненулевое значение — включает его. τ_n^bg и τ_p^bg фиксируются
  вместе. В таблице результата — «фикс.»; галочки сохраняются в наборе.
- `fitting.fit_iv(..., locked=...)`, `fitting.fittable_fields`,
  `fitting.FIELD_TO_PARAM`.

### 3.3.3 — ρ подложки и температура; ссылки на методичку во всех сообщениях

- **Ошибка «ρ_sub больше максимально возможного … решения нет» при нагреве.**
  Причина — программа пересчитывала паспортное ρ подложки в N_A при
  температуре образца T. Наибольшее ρ германия ρ_max = 1/(2q·n_i·√(μ_nμ_p))
  падает с ростом n_i: около 60 Ом·см при 300 К, 13 Ом·см при 330 К,
  поэтому ρ = 40 Ом·см при T = 330 К становилось «невозможным». N_A от
  температуры не зависит, ρ — зависит, и паспортное ρ задано при комнатной
  температуре. Новое поле «T изм. ρ» (по умолчанию 300 К): N_A находится
  по ρ при нём, носители при T — по этому N_A.
- Сообщение об ошибке объясняет причину (собственные носители, формула
  ρ_max, значения при 300 и 330 К) и что сделать.
- `physics.resistivity_max`; окно формул — формула ρ_max; в Справочнике —
  ρ подложки при T и R_sub по нему.
- Каждое сообщение об ошибке и предупреждение заканчивается «Подробнее:
  методичка, п. …» (`hints.ERROR_REFS`, `hints.with_error_reference`):
  ошибки ввода, предупреждения модели во вкладке «Предупреждения»,
  подгонка, оценка τ₀, блок идеальности. Тест проверяет, что пункты есть в
  методичке.

### 3.3.4 — от чего зависит ВАХ: N_D⁺, N_i, граница «соседний слой»

Проверка жалобы «N_D⁺ и N_i меняю на порядки — ВАХ не меняется»:

- **Базовая модель** — так устроено: ток задают n и J₀ формулы (6.3),
  концентрации входят только в V_bi и ВФХ. Теперь это сказано в окне под
  разделом «Легирование и температура».
- **Физическая модель** — ток определяет слабо легированная сторона
  перехода (∝ n_i²/N). В сценарии B это почти собственная подложка, поэтому
  N_i и N_D⁺ почти не влияют на ВАХ (< 3 % на три порядка), а ρ подложки —
  сильно. В сценарии A N_i меняет ток в ~20 раз. Это физика, не ошибка.
- **Найдена ошибка:** при смыкании ОПЗ с границей слоя граничное условие
  «сток» давало ток ∝ 1/w (около 1 А при −3 В в сценарии A с N_i = 10¹⁴).
  Новое граничное условие **«соседний слой»** (4.3а): граница с конечной
  скоростью отвода носителей S = (D₂/L₂)·(n₂₀/n₁₀)·f₂ в следующий слой того
  же типа (с тепловым пределом v_th). Сильнее легированный сосед почти
  отражает, слабее — почти сток; при смыкании ток — поток в соседний слой
  (конечный). Концентрация соседнего слоя (N_D⁺ в сценарии B, ρ подложки в
  сценарии A) теперь входит в ток так, как требует физика.
- «Соседний слой» — умолчание окна для внутренних границ (i/n⁺,
  i/подложка) у новых образцов; `physics.Structure` и эталоны ТЗ §9
  сохраняют условия ТЗ («сток» / «отражение»). Контакты — идеальный «сток».
- `physics.boundary_velocity`, `boundary_factor(u, boundary, r)`,
  `Side.neighbour`; формула (4.3а) в окне формул; тесты чувствительности и
  смыкания.

### 3.3.5 — физика подгонки: физичность прежде точности

- **Правило выбора варианта** (`fitting.select_variant`): из вариантов с
  ошибкой δ ≤ 5 % (`TARGET_ERROR`; согласие 4–8 % достаточно) берётся
  самый простой; только если таких нет — критерий BIC, и программа
  сообщает, что модели чего-то не хватает. Раньше выбирался лучший по BIC
  даже при 1–3 % у более простого варианта. Для ВАХ опорного образца теперь
  выбирается одна нелинейная утечка (δ = 1.7 % эмпирическая, 3.0 %
  физическая) вместо утечки с модуляцией R_s (0.5 %); пояснение называет,
  какой была бы ошибка с модуляцией.
- **Физические границы**: n эмпирической модели — [1, 2] (диффузия …
  рекомбинация в ОПЗ; раньше 0.5 … 8). Параметр на границе — признак
  другого механизма; пояснение говорит какого.
- **Неопределённые параметры**: погрешность по SVD якобиана; параметр, от
  которого невязки не зависят, — «не опр.» (раньше псевдообратная матрица
  давала ложную почти нулевую погрешность, например у R_sh, когда обратный
  ток описывает утечка). Погрешность > 100 % — в пояснениях с советом
  зафиксировать параметр.
- **Проверки согласованности**: J₀ против диффузионного J_s структуры
  (4.3), R_s против сопротивления подложки ρ·d/A, τ₀^bg против τ^bg.
- **Исправлено пояснение о модуляции R_s**: сравнивается сопротивление
  цепи (dV/dI минус n·kT/qI диода), а не сам dV/dI — обычный спад
  сопротивления диода больше не выдаётся за модуляцию.
- `fit_iv(..., target=...)`; `FitResult.target`, `FitResult.at_bounds`;
  тесты правила выбора, границы n, неопределённости и пояснения о модуляции.

## CV-VI-ver.3.2-autoThinking (от CV-VI-ver.3.1-fixes)

Автоподгонка ВАХ с выбором механизмов тока и переработанный интерфейс.
В `main` не влита.

### Почему модель не совпадала с реальной ВАХ

Разбор реальной ВАХ опорного образца показал два механизма, которых не было
в модели:

1. **Обратная ветвь сверхлинейна**: при |V| > 1 В ток растёт как |V|^m,
   m ≈ 2.3. Шунт R_sh даёт только прямую линию. Нужна нелинейная утечка
   I_L·|V|^m; m ≈ 2 соответствует закону Мотта–Гёрни (ток, ограниченный
   пространственным зарядом), 3 < m < 7 — «мягкой» ВАХ по дислокациям.
2. **Верх прямой ветви изгибается**: дифференциальное сопротивление падает с
   ростом тока быстрее, чем nkT/(qI), — модуляция проводимости высокоомной
   базы инжектированными носителями. Введено R_s(I) = R_s/(1 + |I|/I_mod) (6.9).

С обоими механизмами ошибка описания падает с 13.8 % до 0.5 % (эмпирическая
модель) и до 1.1 % (физическая модель) — это уровень округления тока в
файле измерения. Коэффициент идеальности по всей ВАХ с учётом R_s(I) —
1.31, а не 1.65–1.75: прежняя оценка была завышена последовательным
сопротивлением.

### Автоподгонка («Подогнать к ВАХ»)

- Подбирает неизмеряемые параметры: в базовом режиме J₀ и n, в физической
  модели τ₀^bg и τ^bg; в обоих — R_s, R_sh.
- Механизмы (нелинейная утечка, модуляция R_s) подключаются только если
  данные этого требуют: сравниваются четыре варианта модели по критерию
  BIC (6.11), сложный принимается при выигрыше больше 10.
- Результат: значения с погрешностями, ошибка (6.10) по ветвям, таблица
  вариантов и пояснения по форме ВАХ («почему так»), включая физическую
  интерпретацию (закон утечки, сравнение τ₀ с дислокационным временем жизни).
- Найденные значения подставляются в поля (синие); «Вернуть значения до
  подгонки» — откат. Подгонка идёт в фоне: эмпирическая ~1 с, физическая ~12 с.

### Интерфейс

- Слева — шаги работы: 1. режим (с описанием), 2. образец, 3. параметры
  (сворачиваемые разделы; видны только поля текущего режима, «показать все
  поля» — остальные серыми), 4. «Рассчитать», «Подогнать к ВАХ», «Заполнить
  пустые поля».
- Справа — графики и вкладки результатов: «Подгонка и отклонения» (таблица,
  пояснения, график отклонения модели от ВАХ в каждой точке — работает и при
  ручной подгонке), «Идеальность n», «Предупреждения (N)», «Схема мезы».
- Строка статуса внизу: V_bi, n_эксп, n_мод, отклонение от ВАХ δ,
  предупреждения.
- Панель инструментов сгруппирована: данные | наборы | справка и методичка.
- Эквивалентная схема (R_s, I_mod, R_sh, I_L, m) доступна во всех режимах.
- «Автофит» переименован в «Заполнить пустые поля».

### Модель

- `physics.series_resistance` — R_s(I) (6.9); `Structure.I_mod`.
- Решатель (6.1) — бисекция по напряжению на переходе сразу для всех точек:
  корень единственный, быстрее, не зависит от начального приближения.
- n_эксп и проверка R_s учитывают R_s(I).
- `fitting.py` — автоподгонка, формулы (6.10), (6.11).
- Окно «Формулы и параметры»: (6.9)–(6.11), раздел 6.7, источники [BCL99],
  [Sch78], [KR95], [MG40].

### Тесты

Решатель с модуляцией; восстановление параметров на синтетике; отказ от
лишних механизмов; τ₀ физической модели; ВАХ опорного образца из каталога
данных (без него пропускается).

## CV-VI-ver.3.1-fixes (от CV-VI-ver.3.0-Fenix)

Ветка исправлений и доработок интерфейса поверх 3.0. В `main` не влита:
сначала тестирование и локальный запуск.

### Исправления

- **n_эксп «не определён» при загруженной ВАХ.** Причина — завышенное R_s:
  после поправки V − I·R_s напряжение на переходе на верху прямой ветви
  убывало, и окно подгонки схлопывалось. Расчёт стал устойчивым к
  завышенному R_s, шуму и плотным данным. Если n_эксп всё же не найден или
  недостоверен, блок идеальности пишет причину, связь с R_s (V_j = V − I·R_s)
  и что сделать; программа называет предел R_s по наклону dV/dI верха прямой
  ветви. Строка статуса — коротко («R_s завышено»).
- В базовом режиме n и J₀ подставляются из ВАХ, как только n_эксп найден —
  в том числе после исправления R_s.

### Режимы окна

Переключатель внизу окна; значения при переключении сохраняются,
недоступные поля затемнены.

| Режим | Модель | Поля |
|---|---|---|
| Базовая модель | эмпирическая (6.3): n, J₀, R_s, R_sh | минимум: D, кольцо, h, N_D⁺, N_i, T, n, J₀, R_s, R_sh |
| Расширенная модель | физическая | всё измеряемое: геометрия и толщины слоёв, концентрации, ρ подложки, T, R_s, R_sh, N_dis, Холл, АСМ, XRD, технология; граничные условия |
| Подгонка | физическая | плюс неизмеряемое: подвижности неосновных носителей, σ_R, времена жизни, n₂, нелинейная утечка, опции SNS и краевой площади |

- **«Автофит»** (расширенный режим и «Подгонка») заполняет пустые поля:
  n и J₀ — из ВАХ, N_D⁺ ≈ Q/d_n — по дозе имплантации, остальное — значениями
  по умолчанию. Подставленное выделено синим до правки; подсказка поля
  говорит, откуда значение.
- «Наборы → Новый образец (пустые поля)».
- Новые поля «Измерения и технология» (хранятся в наборе, пустое — «не
  измерено»): холловская подвижность, RMS и плотность дефектов по АСМ,
  полуширина кривой качания XRD, доза и энергия имплантации, температуры
  отжига и роста.

### Интерфейс

- Галочки для каждой кривой графиков 1–3.
- Всплывающие подсказки у полей, кривых, режимов, граничных условий и
  строки статуса (V_bi, n_эксп, n_мод); у полей — ссылка на пункт методички
  («Подробнее: методичка, п. 1А.5»). Разница R_s и R_sh, влияние ρ подложки
  на V_bi — в подсказках.
- Ползунки убраны: значение меняется колесом мыши (шаг — десятая часть
  старшего разряда, с Ctrl — в 10 раз мельче).
- Кнопка «Оценить τ₀ по обратной ветви» («Подгонка»).
- Окно «Формулы и параметры»: первая вкладка «Параметры подгонки» — режимы,
  группы полей и их влияние, граничные условия «сток / отражение / длинная
  база», табличные значения μ и σ_R с источниками и объяснением выбора,
  автофит, V_bi, что измеряется и что подбирается. Многострочный текст в
  окне больше не обрезается.

### Модель

- Эмпирическая модель (6.3) для базового режима (`physics.MODEL_EMPIRICAL`).
- `physics.estimate_tau0_from_reverse` — оценка τ₀ по обратной ветви.
- `physics.series_resistance_limit` — предел R_s по наклону dV/dI.
- Наборы: поля `mode`, `n_emp`, `J0_emp` и поля измерений; наборы без `mode`
  открываются в «Подгонке».

### Документация

- README и `mesa_diode/simulator/README.md` — режимы, автофит, подсказки.
- CLAUDE.md — правило сверки изменений с методичкой.
- Методичка (каталог данных) — глава 1А «Работа в программе»: режимы, все
  поля (что это, откуда взять, на что влияет), табличные значения и почему
  они такие, сообщения блока идеальности, автофит, графики, ввод значений.

### Тесты

Без каталога данных (как в CI) — зелёные, тесты с данными пропускаются;
с каталогом данных — все зелёные. Окно проверено в Tk на виртуальном
дисплее (Linux); на Windows не проверялось.
